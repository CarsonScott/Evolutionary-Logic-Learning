from functional_graph import *


class Agent(Dictionary):
	def __init__()


class GraphAgent(FunctionGraph):
	def __init__(self, history_size):
		super().__init__(history_size)
		self.observers = list()

	def __call__(self, inputs):
